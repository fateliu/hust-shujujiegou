# 集合

## 一，要求

- 集合元素是字符串
- 不允许重复
- 数组和链表实现
- c语言

## 二，要求操作
1. 新建空集合
2. 清空集合
3. 添加元素
4. 删除给定元素
5. 并集、交集、差集
6. 子集判断
7. 成员判定
8. 查询集合大小

# 
# 文本排版的设计与实现

## 一、基本要求

- 输入为英文文本文件，由多个文本行或空行组成，文本行中包含空格、单词和标点符号。
- 使用 C 语言实现文本排版，支持空格压缩、分段、自动换行和分页。
- 保持原有单词、标点符号及其顺序，不改变文本内容。
- 设计所需数据结构和算法，并提供测试代码演示功能。

## 二、输入与参数要求

| 参数 | 含义 | 默认值 |
| --- | --- | --- |
| 首行缩进 | 每个段落第一行开头的空格数 | 4 |
| 页长 | 每页最多容纳的排版行数 | 30 |
| 页宽 | 每行最多容纳的字符数，包含空格和标点 | 80 |

- 支持读取输入文件并设置排版参数；未设置的参数采用默认值。
- 参数必须为整数，页长、页宽大于 0，缩进满足 `0 ≤ 缩进 < 页宽`。
- 输入文件无法读取、参数不合法或输出文件无法写入时，给出明确提示。

## 三、功能要求

### 1. 分段与空行处理

- 对输入文本进行分段。
- 多个连续空行压缩为一个空行。
- 题目未明确段落边界，本设计约定：以空行分隔段落，同一段落内的原始换行作为单词间的分隔，重新按页宽排版。
- 仅包含空格的行也视为空行；忽略文件开头、结尾的空行，不生成空段落。

### 2. 空格压缩

- 文本行内多个连续空格压缩为一个空格。
- 去掉原始行首、行尾的多余空格，首行缩进由程序统一添加。
- 排版后的行末不保留多余空格；规定的首行缩进不参与空格压缩。

### 3. 换行与对齐

- 每行字符数不得超过页宽，首行缩进也计入字符数。
- 每个段落的首行缩进，其余行左对齐，不为右侧对齐补充空格。
- 将单词及其右侧紧邻的标点符号视为不可拆分的整体，不允许跨两行。
- 当前行无法容纳下一个整体时，将其移至下一行；恰好能容纳时不提前换行。
- 换行后的正文前不保留单词间的分隔空格。

### 4. 分页

- 按排版后的行数分页，每页不超过指定页长。
- 本设计约定：正文行和段间空行都计入页长，段间空行即使位于页首或页尾也保留。
- 一个段落可以跨页，跨页后的续行不重复进行首行缩进。
- 本设计采用换页符 `\f` 分隔相邻页面，换页符不计入正文行数；最后一页之后不追加换页符。

### 5. 输出与特殊情况处理

- 将排版结果写入输出文本文件，并提示输出位置。
- 空文件或仅含空白行的文件输出为空，不生成多余页面。
- 输入文件最后一行没有换行符时，也应完整处理。
- 对超长单词，题目未规定处理方式。本设计约定：若单词及其紧邻标点长于页宽，或段落首词长于“页宽减去缩进”，则提示当前参数无法完成排版，建议增大页宽或减小缩进，不拆分单词、不超宽输出。

## 四、测试需求

测试程序应构造不同输入和参数，调用排版功能并核对输出，覆盖以下情况：

| 测试场景 | 预期结果 |
| --- | --- |
| 使用默认参数处理普通英文文本 | 按缩进 4、页长 30、页宽 80 排版 |
| 自定义缩进、页长、页宽 | 输出符合指定参数 |
| 多个连续空格 | 压缩为一个空格 |
| 多个空行、仅含空格的行 | 合并为一个段间空行 |
| 同一段落包含多个原始文本行 | 合并后按页宽重新换行 |
| 单词紧邻逗号、句号等标点 | 单词和标点不被拆到两行 |
| 下一单词恰好能放入当前行 | 不提前换行 |
| 下一单词放不入当前行 | 整体移到下一行 |
| 行数恰好达到页长、超过页长 | 正确分页，不产生多余末页 |
| 段落跨页、段间空行位于分页边界 | 续行不重复缩进，空行正常计数 |
| 缩进为 0、页长为 1 | 正确处理合法边界参数 |
| 空文件、全空白文件、末行无换行符 | 正确处理，末行内容不丢失 |
| 超长单词或首词无法放入首行 | 给出无法按当前参数排版的提示 |
| 参数非法、文件读取或写入失败 | 给出对应错误提示 |


# 基于磁盘的外部排序的设计与实现

## 一基本要求

- 输入为保存在磁盘文件中的整数序列，数据由程序随机生成。
- 数据量可能超过内存容量，不能一次性读入全部数据。
- 使用 C 语言实现外部排序，将结果写入输出文件。
- 本设计约定按整数从小到大排序，保留重复数据。

## 功能

1. 随机数据生成：支持指定整数数量和取值范围，生成输入文件。
2. 分块读取：根据设定的内存容量，将输入数据分批读入内存。
3. 块内排序：对每批数据进行内部排序，生成有序块。
4. 临时文件存储：将有序块写入磁盘临时文件。
5. 有序块合并：合并多个有序块；有序块过多时分多轮合并。
6. 结果输出：将最终有序序列写入结果文件。
7. 资源清理：排序结束后关闭文件，清理临时文件并释放内存。
8. 异常处理：对文件操作失败、参数不合法等情况给出提示。

## 设计

- 设计输入文件、临时文件和结果文件的数据存储格式。
- 设计内存中的排序数组及合并所需缓冲区。
- 合理限制内存使用量和同时打开的文件数量。
- 分析块内排序与合并算法的时间复杂度、空间复杂度。
- 分析有序块数量、合并轮数及磁盘读写量，说明内存容量对 I/O 代价的影响。

## 四、测试

- 测试空文件、单个整数和重复整数。
- 测试包含负数、已排序及逆序的数据。
- 测试数据量小于、等于和大于单块容量的情况。
- 测试需要多轮合并的情况。
- 检查结果是否有序，数据数量及各整数出现次数是否保持不变。
- 提供测试代码和代表性运行结果。

# 数独游戏的设计与实现（选做）

## 一、基本要求

- 使用 C 语言实现基于文本界面的 9×9 标准数独游戏。
- 收集并阅读数独规则资料，明确行、列及九宫格的限制。
- 每个空格填写 1～9 的数字，完成后每行、每列和每个 3×3 宫内的数字均不重复。
- 提供入门、中等、复杂三个难度模式。

## 二、功能要求

1. 难度选择：玩家可以选择不同难度开始游戏。
2. 题目生成：根据所选难度生成初始棋盘，保证题目有解；本设计要求解唯一。
3. 棋盘显示：用文本显示棋盘、行列编号及九宫格分隔，区分初始数字和玩家填写的数字。
4. 数字填写：玩家输入行号、列号和数字，填写指定位置。
5. 修改与清除：允许修改或清除玩家填写的数字，不允许修改初始数字。
6. 输入检查：对坐标越界、数字不合法等输入给出提示。
7. 冲突检测：检查填写数字是否与同行、同列或同宫的数字重复，并提示冲突。
8. 完成判断：棋盘填满且满足数独规则时，提示游戏完成。
9. 退出游戏：允许玩家主动退出。

## 三、设计要求

- 使用二维数组保存棋盘，另行记录初始数字的位置。
- 设计题目生成、有效性检查、冲突检测及完成判断算法。
- 明确三个难度等级的划分标准，不能仅凭随机生成就认定难度不同。
- 文本界面应提供清晰的操作说明和反馈。

## 四、测试要求

- 检查生成棋盘及其解是否符合数独规则。
- 测试正常填写、修改、清除及退出操作。
- 分别测试行冲突、列冲突和宫内冲突。
- 测试修改初始数字、非法坐标和非法数字。
- 测试正确完成后的胜利提示。
- 每个难度至少提供一个从初始棋盘到完成棋盘的完整游戏过程例子。